#include <stdio>
/**
 * main - a program that print a line using the printf fuction
 * Return: 0(success)
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n")
return (-1);
}

