#include <stdio.h>

/**
 * main - prints "programming is like bulding a multilingual 
 * puzzle, followed by a new line."
 * Return: Always 0.
 */
int main (void)

{
	puts ("\"programming is like building a multilingual puzzle, followed by a new line");

	return (0);
}

